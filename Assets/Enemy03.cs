using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy03 : EnemyBaseScript
{
    // Start is called before the first frame update
    void Start()
    {
        EnemyLife = 999;
        Debug.Log(EnemyLife);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
